# Vertical App List

The full app list is user-initiated through the configured edge interaction. It supports fast alphabet navigation, accessibility labels, work-profile awareness where supported, and hundreds of installed apps.