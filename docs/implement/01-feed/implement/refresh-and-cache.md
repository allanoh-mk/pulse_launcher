# Refresh and Cache

Use event-driven updates first. Cache network-backed content with timestamps and source identity. A disabled provider performs no periodic work. Connectivity speed sampling is bounded and never runs continuously merely to animate a number.