# Deletion Gates

Required evidence: no Gradle dependency, source reference, manifest registration, resource reference, test/CI dependency; then clean build and supported-device smoke tests. Folder names are not evidence.