# Android App Widgets

AppWidgetHost integration must preserve host IDs, binding permissions, restore behavior, resize constraints, and provider failure states.

Widget hosting is optional per layout and must not block Bento startup.