# Bento Layout Editor

The editor supports add, remove, duplicate, reorder, resize, configure, undo, redo, and reset.

Edits are staged so a failed widget bind or provider load cannot corrupt the saved layout.