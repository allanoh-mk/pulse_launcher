# Bento Interactions

Supported action triggers: tap, double-tap, swipe, long press, and provider-specific interaction.

Interaction maps are validated so imported layouts cannot invoke arbitrary code.