# Bento Default Layout

Initial proposal:
Weather → Music/Battery → Calendar → Connectivity/Screen Time → Smart Apps.

The default is editable, removable, and resettable. It is a starting point, not a locked dashboard.